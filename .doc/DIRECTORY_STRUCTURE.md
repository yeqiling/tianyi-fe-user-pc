# 项目目录结构说明

## 📁 目录组织

```
src/
├── assets/           # 静态资源
│   ├── images/       # 图片资源
│   ├── icons/        # 图标资源
│   ├── fonts/        # 字体文件
│   └── videos/       # 视频资源
├── components/       # 组件
│   ├── ui/          # 基础UI组件 (Button, Input, Modal等)
│   ├── layout/      # 布局组件 (Header, Footer, Sidebar等)
│   └── forms/       # 表单组件
├── hooks/           # 自定义Hooks
├── pages/           # 页面组件 (可选，配合routes使用)
├── routes/          # TanStack Router路由文件
├── services/        # API服务和数据获取
├── stores/          # 状态管理 (TanStack Store)
├── types/           # TypeScript类型定义
├── utils/           # 工具函数
├── constants/       # 常量定义
├── styles/          # 样式文件
│   ├── components/  # 组件样式
│   ├── pages/       # 页面样式
│   └── themes/      # 主题相关
└── i18n/           # 国际化文件

public/
├── images/          # 公共图片
├── icons/           # 公共图标
└── fonts/           # 公共字体
```

## 🎯 使用指南

### 组件开发
- **UI组件**: 放在 `components/ui/` 下，专注于样式和交互
- **布局组件**: 放在 `components/layout/` 下，处理页面布局
- **业务组件**: 直接放在 `components/` 下或按功能分组

### 样式管理
- 使用CSS变量进行主题管理 (`styles/themes/variables.css`)
- 组件样式放在 `styles/components/` 下
- 页面特定样式放在 `styles/pages/` 下

### 状态管理
- 使用TanStack Store进行状态管理
- 全局状态放在 `stores/` 目录下

### 路由
- 使用TanStack Router的文件路由系统
- 路由文件放在 `routes/` 目录下
- 页面组件可以放在 `pages/` 目录下 (可选)

### 资源管理
- 静态资源优先放在 `src/assets/` 下 (会被打包优化)
- 需要直接访问的资源放在 `public/` 下
